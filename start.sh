# This command will rebuild and restart the application.
#
# After running this command, open your browser to "localhost" to view the application.
# It may take a while to build.  Wait until the long stream of messages in your command
# line console comes to an end before viewing the application in your browser.
#
docker compose up --build
